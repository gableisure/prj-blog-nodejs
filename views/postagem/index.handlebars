<div class="card">
    <div class="card-body">
        <h2 class="py-1">{{postagem.titulo}}</h2>
        <hr>
        <small>Data de publicação: {{postagem.date}}</small>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>

